# MonkeyShop 容器化适配 Docker 全记录

## 一、项目概述

MonkeyShop 是一个基于 Spring Boot 3.2 + Vue 3 + MySQL 8.0 的灵长类商品交易商城。项目采用单体架构，后端同时托管前端静态页面和 RESTful API，通过 Docker + Docker Compose 实现容器化部署。

---

## 二、容器化架构

```
┌──────────────────── Docker Compose ────────────────────┐
│                                                         │
│  ┌──────────────────────┐    ┌────────────────────────┐ │
│  │   monkey-app         │    │   monkey-mysql         │ │
│  │   (Java 21 / SB 3.2) │    │   (MySQL 8.0)         │ │
│  │                      │    │                        │ │
│  │   Port: 8888         │───▶│   Port: 3306           │ │
│  │   TZ: Asia/Shanghai  │    │   TZ: Asia/Shanghai    │ │
│  │   Charset: utf8mb4   │    │   Charset: utf8mb4     │ │
│  │                      │    │                        │ │
│  │   /data/images/      │    │   /var/lib/mysql       │ │
│  │     ↕ (bind mount)   │    │     ↕ (named volume)   │ │
│  └──────┬───────────────┘    └────────┬───────────────┘ │
│         │                            │                  │
└─────────┼────────────────────────────┼──────────────────┘
          │                            │
          ▼                            ▼
   ./uploads/                    mysql_data 卷
   (宿主机持久化)                (Docker 管理持久化)
```

---

## 三、适配 Docker 所做的全部努力

### 3.1 Dockerfile 适配

#### (1) 多阶段构建（编译内置化）

```dockerfile
# 阶段1: Maven 编译
FROM maven:3.9-eclipse-temurin-21 AS builder
WORKDIR /build
COPY pom.xml .
RUN mvn dependency:go-offline -B    # 利用缓存层，依赖不变时跳过下载
COPY src ./src
RUN mvn clean package -DskipTests -B

# 阶段2: 运行时镜像
FROM openjdk:21-jdk-slim
COPY --from=builder /build/target/*.jar app.jar   # 从编译阶段拷贝 JAR
```

**为什么用多阶段构建？**
- 旧方案 `COPY target/*.jar` 依赖宿主机预先 `mvn package`，忘了打包就部署旧代码
- 多阶段构建将编译内置到 Docker 中，`docker compose up -d --build` 一条命令搞定
- `pom.xml` 单独 COPY + `dependency:go-offline` 利用 Docker 缓存层，依赖不变时无需重新下载
- 最终运行时镜像不包含 Maven 和源码，体积更小

#### (2) 国内镜像源加速

```dockerfile
FROM swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/openjdk:21-jdk-slim
```

使用华为云 SWR 加速源拉取基础镜像，解决国内拉取 Docker Hub 镜像超时的问题。

#### (2) APT 源替换为阿里云

```dockerfile
RUN sed -i 's/deb.debian.org/mirrors.aliyun.com/g' /etc/apt/sources.list.d/debian.sources
RUN sed -i 's/deb.debian.org/mirrors.aliyun.com/g' /etc/apt/sources.list
```

容器内 apt-get 默认使用 Debian 官方源，国内构建时经常连不上，替换为阿里云源。

#### (3) 字体库安装（验证码功能依赖）

```dockerfile
RUN apt-get install -y fontconfig libfreetype6 fonts-dejavu fonts-liberation
```

验证码生成使用 Java AWT 的 `Graphics2D`，在无头（headless）的 Docker 容器中需要：
- `fontconfig` + `libfreetype6`：字体渲染基础设施
- `fonts-dejavu`：默认字体族
- `fonts-liberation`：Arial 的开源替代字体，保证验证码文字渲染与本地开发一致

如果不安装字体库，验证码接口会返回空白图片或抛出异常。

#### (4) 容器时区设置

```dockerfile
ENV TZ=Asia/Shanghai
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone
```

Docker 容器默认时区为 UTC，导致：
- MySQL 的 `CURRENT_TIMESTAMP` 按 UTC 记录
- Java 的 `LocalDateTime.now()` 按 UTC 生成
- 管理后台统计图表的"近7天"数据偏移 8 小时

设置时区后，容器内所有时间相关操作统一为北京时间。

#### (5) 启动脚本（默认图片初始化）

```dockerfile
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
```

这是解决"默认图片黑屏"问题的关键适配。详见 3.3 节。

---

### 3.2 docker-compose.yml 适配

#### (1) MySQL 健康检查 + 条件依赖

```yaml
mysql:
  healthcheck:
    test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
    timeout: 20s
    retries: 10

myshop:
  depends_on:
    mysql:
      condition: service_healthy
```

确保 MySQL 完全就绪后 Java 应用才启动，避免应用启动时数据库连接失败。

#### (2) 环境变量注入（配置外部化）

```yaml
myshop:
  environment:
    DB_URL: jdbc:mysql://mysql:3306/monkeyshop?...
    DB_USERNAME: ${MYSQL_USER}
    DB_PASSWORD: ${MYSQL_PASSWORD}
    SERVER_PORT: 8888
    SPRING_JPA_SHOW_SQL: "false"
    APP_UPLOAD_PATH: /data/images/
    TZ: Asia/Shanghai
```

利用 Spring Boot 的环境变量覆盖机制（`SPRING_` 前缀自动映射），无需修改代码即可切换：
- 数据库连接地址（`localhost` → `mysql` 容器内 DNS）
- 数据库用户名/密码
- JPA SQL 日志开关（容器中关闭，避免性能损耗）
- 图片上传路径（`APP_UPLOAD_PATH` 自定义属性）
- 时区

#### (3) 数据持久化

```yaml
mysql:
  volumes:
    - mysql_data:/var/lib/mysql    # 命名卷持久化数据库

myshop:
  volumes:
    - ./uploads:/data/images       # 绑定挂载持久化上传文件
```

- MySQL 使用 Docker 命名卷，数据不随容器销毁而丢失
- 上传文件使用绑定挂载，宿主机可直接访问和备份

#### (4) 端口冲突规避

```yaml
mysql:
  ports:
    - "3307:3306"    # 宿主机 3307，避免与本地 MySQL 冲突
```

#### (5) 应用专用数据库用户

```yaml
MYSQL_USER: monkeyuser
MYSQL_PASSWORD: <app-db-password>
```

创建专用用户而非使用 root，遵循最小权限原则。

#### (6) 容器重启策略

```yaml
restart: unless-stopped
```

服务器重启后容器自动恢复，无需手动干预。

#### (7) 密码敏感信息 .env 文件管理

```yaml
MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
MYSQL_PASSWORD: ${MYSQL_PASSWORD}
```

密码等敏感信息从 docker-compose.yml 中抽离到 `.env` 文件，`.env` 已加入 `.gitignore`，不会被提交到版本库。

#### (8) MySQL 时区与字符集

```yaml
mysql:
  environment:
    TZ: Asia/Shanghai
  command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --default-time-zone=+08:00
```

- `TZ=Asia/Shanghai`：影响容器 Linux 系统时区
- `--default-time-zone=+08:00`：**直接设置 MySQL 服务内部时区**，这是关键配置
- `--character-set-server=utf8mb4`：字符集支持中文和 emoji

> ⚠️ 仅设置 `TZ` 环境变量**不够**，MySQL 8.0 的 `CURRENT_TIMESTAMP` 取的是 MySQL 服务内部时区，而非容器系统时区。如果不加 `--default-time-zone`，MySQL 可能仍用 UTC，导致 `create_time` 字段存 UTC 时间，而 Java 用上海时间比较，管理后台数据看板显示为空白。

---

### 3.3 管理后台数据看板空白修复

#### 问题描述

容器化部署后，管理后台数据看板只显示默认数据（0订单0访问），用户下单后数据不更新，整个看板区域变白。历史总订单量和历史总访问量的图标也出现异常。

#### 根因分析

**问题1：MySQL 时区不一致**

Order 实体的 `createTime` 字段使用 `insertable=false, updatable=false`，完全由 MySQL 的 `DEFAULT CURRENT_TIMESTAMP` 自动生成。如果 MySQL 内部时区为 UTC，存的时间就是 UTC，而 Java 端用 `serverTimezone=Asia/Shanghai` 读取时会被 JDBC 驱动转换，导致时间比较逻辑出错。

```
MySQL 存储时间: 2026-06-02 07:00:00 (UTC)
JDBC 读取转换: 2026-06-02 15:00:00 (Asia/Shanghai)  ← 看似正确
但 LocalDate.now() 在容器中可能取 UTC: 2026-06-01  ← 跨天了！
导致 "近7天" 过滤条件匹配不到数据
```

**问题2：StatsController 缺少 null 安全检查**

`o.getCreateTime()`、`o.getPrice()`、`o.getStatus()` 在某些场景下可能为 null，导致 stream 操作抛出 NPE，前端收到 500 错误后整个看板变白。

#### 解决方案

1. docker-compose.yml 中 MySQL 加 `--default-time-zone=+08:00`
2. StatsController 中 `LocalDate.now()` 改为 `LocalDate.now(ZoneId.of("Asia/Shanghai"))`
3. 所有 stream 操作增加 null 检查

---

### 3.3 默认图片黑屏问题修复

#### 问题描述

容器化部署后，首页背景图、轮播图、商品默认图、用户默认头像全部显示为黑色或空白。

#### 根因分析

项目使用 Spring Boot 的 `ResourceHandler` 配置了双位置图片查找：

```java
registry.addResourceHandler("/images/**")
    .addResourceLocations(
        "file:" + UPLOAD_PATH,       // 优先：外部挂载卷
        "classpath:/static/images/"  // 兜底：JAR 包内
    );
```

容器环境中 `UPLOAD_PATH` 为 `/data/images/`（挂载卷），但挂载卷初始为空，默认图片只存在于 JAR 包内的 `classpath:/static/images/`。Spring Boot 可执行 JAR 的 classpath 资源解析在 `file:` + `classpath:` 混合配置下存在回退不可靠的问题，导致默认图片无法加载。

#### 解决方案：entrypoint.sh 启动时初始化

```bash
#!/bin/bash
IMAGE_DIR="/data/images"
JAR_IMAGES="BOOT-INF/classes/static/images"

if [ -f app.jar ]; then
    unzip -o app.jar "$JAR_IMAGES/*" -d /tmp/jar_extract/ > /dev/null 2>&1
    if [ -d "/tmp/jar_extract/$JAR_IMAGES" ]; then
        cp -rn /tmp/jar_extract/$JAR_IMAGES/* "$IMAGE_DIR/" 2>/dev/null || true
    fi
    rm -rf /tmp/jar_extract/
fi

exec java -jar app.jar
```

每次容器启动时：
1. 从 JAR 包中解压默认图片到临时目录
2. 使用 `cp -rn`（不覆盖已存在文件）复制到挂载卷
3. 清理临时文件
4. 启动 Java 应用

这样确保挂载卷中始终有完整的默认图片，同时不会覆盖用户已上传的新文件。

---

### 3.4 后端代码适配

#### (1) 图片存储路径配置化

**修改前**（硬编码 OS 检测）：

```java
public static final String UPLOAD_PATH = isLocalDev()
    ? System.getProperty("user.dir") + "/src/main/resources/static/images/"
    : "/data/images/";
```

**修改后**（配置属性注入 + OS 检测兜底）：

```java
@Value("${app.upload-path:}")
private String configuredUploadPath;

public String getUploadPath() {
    if (configuredUploadPath != null && !configuredUploadPath.isEmpty()) {
        return configuredUploadPath;    // 优先使用配置值
    }
    String os = System.getProperty("os.name").toLowerCase();
    if (os.contains("win") || os.contains("mac")) {
        return detectLocalPath();       // 本地开发自动检测
    }
    return "/data/images/";             // 容器默认路径
}
```

优先级：`app.upload-path 配置` > `OS 自动检测` > `容器默认路径`

Docker 环境通过环境变量 `APP_UPLOAD_PATH=/data/images/` 注入，本地开发留空自动检测。

#### (2) JPA SQL 日志容器中关闭

```yaml
SPRING_JPA_SHOW_SQL: "false"
```

`application.properties` 中默认关闭 SQL 日志，避免大量 SQL 日志影响性能。

---

### 3.5 前端代码适配

#### (1) CDN 源替换为国内镜像

**修改前**（国外 CDN，国内服务器可能无法访问）：

```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
<script src="https://unpkg.com/vue@3/dist/vue.global.js">
<link href="https://fonts.googleapis.com/css2?family=Inter:...">
```

**修改后**（国内 CDN 镜像）：

| 资源 | 原 CDN | 替换后 CDN |
|------|--------|-----------|
| Bootstrap CSS/JS | cdn.jsdelivr.net | cdn.bootcdn.net |
| Bootstrap Icons | cdn.jsdelivr.net | cdn.bootcdn.net |
| ECharts | cdn.jsdelivr.net | cdn.bootcdn.net |
| Vue 3 | unpkg.com | cdn.bootcdn.net |
| Google Fonts | fonts.googleapis.com | 保持不变 |

如果不替换，国内服务器部署时 jsdelivr/unpkg/googleapis 可能被墙或极慢，导致整站无样式、无框架、无图标。

#### (2) 首页背景图 CSS 兜底

**修改前**：

```css
.bg-wrapper { background-image: url('/images/monkey.png'); }
```

**修改后**：

```css
.bg-wrapper { background-color: #1a1a2e; background-image: url('/images/monkey.png'); }
```

CSS 的 `background-image` 没有 `onerror` 兜底机制。添加 `background-color` 后，即使背景图加载失败，页面也会显示深色背景而非纯黑，配合遮罩层仍能保证文字可读。

#### (3) API 与图片路径使用相对路径

所有前端页面统一使用相对路径（`/api/xxx`、`/images/xxx`），而非绝对路径（`http://localhost:8888/...`），天然适配容器部署，无需修改。

#### (4) 图片 onerror 兜底

```html
<img :src="monkey.imageUrl" :alt="monkey.name || '商品图片'" onerror="this.onerror=null;this.src='/images/default_product.png'">
```

所有动态图片都配置了 `onerror` 兜底，加载失败时回退到默认图片。配合 entrypoint.sh 的默认图片初始化，确保兜底图片在容器中始终可用。

---

### 3.6 .gitignore 适配

新增忽略项：

```gitignore
.env                # 环境变量文件（含密码）
uploads/avatar/     # 运行时上传的头像
uploads/product/    # 运行时上传的商品图
```

确保敏感信息和运行时生成的文件不被提交到版本库。

---

## 四、部署指南

### 4.1 前置条件

- Docker 20.10+
- Docker Compose V2+

### 4.2 一键部署

```bash
# 1. 克隆项目
git clone <repo-url>
cd JavaScript_MonkeyShop

# 2. 配置环境变量（首次部署需要）
cp .env.example .env
# 编辑 .env 修改密码

# 3. 编译打包
mvn clean package -DskipTests

# 4. 构建并启动
docker compose up -d --build

# 5. 查看日志
docker compose logs -f myshop
```

### 4.3 访问地址

| 页面 | 地址 |
|------|------|
| 首页/登录 | http://localhost:8888 |
| 商城 | http://localhost:8888/shop.html |
| 管理后台 | http://localhost:8888/admin.html |
| MySQL | localhost:3307 |

### 4.4 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 使用外部化初始化密码 |

### 4.5 常用命令

```bash
# 启动
docker compose up -d

# 停止
docker compose down

# 重新构建（代码更新后）
docker compose up -d --build

# 查看日志
docker compose logs -f myshop

# 进入应用容器
docker exec -it monkey-app bash

# 进入数据库
docker exec -it monkey-mysql mysql -umonkeyuser -p"$MYSQL_PASSWORD" monkeyshop
```

---

## 五、适配清单总览

| 维度 | 适配项 | 严重程度 | 状态 |
|------|--------|----------|------|
| Dockerfile | 华为云 SWR 镜像加速 | 🟡 中 | ✅ |
| Dockerfile | 阿里云 APT 源替换 | 🟡 中 | ✅ |
| Dockerfile | 字体库安装（验证码依赖） | 🔴 高 | ✅ |
| Dockerfile | Liberation 字体（Arial 替代） | 🟡 中 | ✅ |
| Dockerfile | 容器时区设置 | 🟡 中 | ✅ |
| Dockerfile | entrypoint 启动脚本 | 🔴 高 | ✅ |
| Compose | MySQL 健康检查 + 条件依赖 | 🔴 高 | ✅ |
| Compose | 环境变量注入（配置外部化） | 🔴 高 | ✅ |
| Compose | 数据持久化（卷挂载） | 🔴 高 | ✅ |
| Compose | 端口冲突规避 | 🟡 中 | ✅ |
| Compose | 应用专用数据库用户 | 🟡 中 | ✅ |
| Compose | 容器重启策略 | 🟡 中 | ✅ |
| Compose | 密码 .env 文件管理 | 🟡 中 | ✅ |
| Compose | MySQL 时区 + 字符集 | 🟡 中 | ✅ |
| Compose | MySQL --default-time-zone | 🔴 高 | ✅ |
| Compose | JPA SQL 日志容器中关闭 | 🟡 中 | ✅ |
| 后端 | 图片存储路径配置化 | 🟡 中 | ✅ |
| 后端 | 默认图片初始化到挂载卷 | 🔴 高 | ✅ |
| 后端 | StatsController 时区安全 | 🔴 高 | ✅ |
| 后端 | StatsController null 安全 | 🔴 高 | ✅ |
| 前端 | CDN 源替换为国内镜像 | 🔴 高 | ✅ |
| 前端 | 首页背景图 CSS 兜底 | 🟡 中 | ✅ |
| 前端 | API/图片使用相对路径 | 🟢 低 | ✅ |
| 前端 | 图片 onerror 兜底 | 🟢 低 | ✅ |
| 其他 | .gitignore 适配 | 🟢 低 | ✅ |

---

## 六、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `Dockerfile` | 修改 | 改为多阶段构建、新增字体库、时区、entrypoint |
| `docker-compose.yml` | 修改 | 新增重启策略、时区、环境变量、.env、MySQL default-time-zone |
| `entrypoint.sh` | 新增 | 容器启动时初始化默认图片 |
| `.env` | 新增 | 敏感配置外部化 |
| `.gitignore` | 修改 | 新增 .env 和 uploads 忽略 |
| `application.properties` | 修改 | 新增 app.upload-path 配置 |
| `WebConfig.java` | 修改 | 图片路径改为配置属性注入 |
| `StatsController.java` | 修改 | 时区安全 + null 安全修复 |
| `index.html` | 修改 | CDN 替换 + CSS 背景色兜底 |
| `shop.html` | 修改 | CDN 替换 |
| `admin.html` | 修改 | CDN 替换 |
| `orders.html` | 修改 | CDN 替换 |
| `profile.html` | 修改 | CDN 替换 |
