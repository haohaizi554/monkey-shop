auth_enabled: false

server:
  http_listen_address: 127.0.0.1
  http_listen_port: 3100
  grpc_listen_address: 127.0.0.1
  grpc_listen_port: 19095

common:
  instance_addr: 127.0.0.1
  path_prefix: ${MONKEYSHOP_RUNTIME_ROOT}/data/loki
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory
  storage:
    filesystem:
      chunks_directory: ${MONKEYSHOP_RUNTIME_ROOT}/data/loki/chunks
      rules_directory: ${MONKEYSHOP_RUNTIME_ROOT}/data/loki/rules

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

compactor:
  working_directory: ${MONKEYSHOP_RUNTIME_ROOT}/data/loki/compactor
  retention_enabled: true
  delete_request_store: filesystem

limits_config:
  retention_period: 4320h
  allow_structured_metadata: true
  volume_enabled: true

analytics:
  reporting_enabled: false
